// const interval = require("../src/interval");
var expect = require("chai").expect;
const Klines = require("../src/klines");

describe("class Klines", () => {
  it("Klines cannot have a length greate then limit", () => {});
  it("Klines can add elements in the end of array");
  it("Klines can remove elements from the end of array");
  it("Klines can add elements in the begin of array");
  it("Klines can remove elements in the begin of array");
  it("Klines can add elements in the begin of array");
});
